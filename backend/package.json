{
  "name": "echo-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight mock API for the Echo prototype",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}

